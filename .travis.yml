language: java
jdk:
  - oraclejdk8
  - openjdk8

before_install:
 - chmod +x gradlew
 - chmod +x gradle/wrapper/gradle-wrapper.jar
install:
 - ./gradlew setupCIWorkspace -S
script:
 - ./gradlew build -S